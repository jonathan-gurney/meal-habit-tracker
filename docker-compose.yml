services:
  meal-habit-tracker:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - meal_habit_tracker_data:/app/data
    environment:
      - PORT=3001

volumes:
  meal_habit_tracker_data:
